[project]
name = "net-worth-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastexcel>=0.13.0",
    "polars>=1.27.1",
]

[dependency-groups]
dev = [
    "ruff>=0.11.7",
]
